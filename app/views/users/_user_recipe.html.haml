%p
  %strong
    Recipes
  - if @recipes.blank?
    %p User has not submitted any recipes yet.
  - else
    %p
      %strong= pluralize(@user.recipes_count, 'recipe')
    %ul
      - @recipes.each do |recipe|
        %li
          = link_to recipe.title, recipe_path(recipe)
          - if recipe.approved == 0
            %span.pending
              Approval Pending