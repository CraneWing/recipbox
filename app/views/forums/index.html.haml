= title('The Forum')
.container
  .panel.panel-info
    - @topics.each do |topic|
      .panel-heading
        = link_to topic.title, topic_path(topic)
      .panel-body
        = topic.description

      