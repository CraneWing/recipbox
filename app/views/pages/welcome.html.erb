 <div class="container-full" id="home-subhead">
   <h1>RecipeBox's Mission is simple...</h1>
   <div class="row" id="subhead"> 
      <div class="col-md-6">
         <h2>1. Discover</h2>
         <p>Provide a place for cooks worldwide to search for recipes and get new ideas for food every day. Get new ideas for your kitchen from our wide and varied recipe collection.</p> 
      </div>

      <div class="col-md-6">
         <h2>2. Share</h2>
         <p>Offer food enthusiasts a platform on which they can feature their own recipes. Register today to upload and share your favorite culinary creations!</p>
      </div>
   </div>
   <hr>
   <div class="row">
      <div class="col-md-6">
        <h1>Newest Recipes</h1>
        <% @recipes.each do |recipe| %>
          <p>
            <%= image_tag recipe.recipe_img, class: 'microthumb' %> <%= link_to recipe.title, recipe_path(recipe) %> Added <%= timeago(recipe.created_at) %>
          </p>
        <% end %>
      </div>
      <div class="col-md-6">
        <h1>Newest Members</h1>
        <% @users.each do |user| %>
          <p>
          <% if user.avatar.blank? || user.avatar == 'avatar-default.jpg' %>
            <%= image_tag "avatar-default.jpg", class: 'microthumb' %>
          <% else %>
            <%= image_tag user.avatar.url(:thumb), class: 'microthumb' %>
          <% end %>
          <%= link_to user.username, user_path(user) %> Joined <time class="timeago"><%= user.created_at %></time>
          </p>
        <% end %>
      </div>
   </div>
</div>
<hr>
   
   
